# TrendIQ Backend
Backend for TrendIQ project which collects data, preprocesses it, trains an LSTM model, and predicts market trends.

## Usage
1. Run `data_collector.py` to collect market data.
2. Run `preprocess.py` to preprocess data.
3. Run `model.py` to train the model.
4. Run `predict.py` to get predictions.
